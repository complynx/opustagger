[package]
name = "opustagger"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
description = "Edit every OpusTag and embedded album cover without re-encoding audio"
license = "MIT"

[lib]
name = "opustagger"
path = "src/lib.rs"

[[bin]]
name = "opustagger"
path = "src/main.rs"

[dependencies]
